apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: authentik
  namespace: flux-system
spec:
  interval: 10m
  path: ./gitops/clusters/homelab/security/authentik
  prune: true
  wait: true
  timeout: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  
  dependsOn:
    - name: traefik
    - name: cert-manager
    - name: postgres-main
  decryption:
    provider: sops
    secretRef:
      name: sops-age
