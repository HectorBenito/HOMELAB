apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catchall-local-nasbenito
  namespace: traefik
  annotations:
    traefik.ingress.kubernetes.io/router.priority: "1"
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - "*.local.nasbenito.com"
    secretName: wildcard-local-nasbenito-com-tls
  rules:
  - host: "*.local.nasbenito.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: default-404
            port:
              name: http      