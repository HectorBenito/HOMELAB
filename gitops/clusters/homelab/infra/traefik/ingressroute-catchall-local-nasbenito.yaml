apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: catchall-local-nasbenito
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: - match: Host(`*.local.nasbenito.com`) && PathPrefix(`/`)
      kind: Rule
      priority: 10
      services:
        - name: default-404
          port: 80
  tls:
    secretName: wildcard-local-nasbenito-com-tls
    domains:
      - main: "*.local.nasbenito.com"