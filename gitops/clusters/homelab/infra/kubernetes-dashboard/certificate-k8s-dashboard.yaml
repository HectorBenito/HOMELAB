apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k8s-dashboard-tls
  namespace: traefik
spec:
  secretName: k8s-dashboard-tls
  dnsNames:
    - kube.local.nasbenito.com
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer