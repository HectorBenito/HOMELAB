apiVersion: v1
kind: Service
metadata:
  name: jetkvm-cloud
  namespace: jetkvm
spec:
  type: ClusterIP
  selector:
    app: jetkvm-cloud
  ports:
    - name: http
      port: 3000
      targetPort: http